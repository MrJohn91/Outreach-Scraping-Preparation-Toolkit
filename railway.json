{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash build.sh"
  },
  "deploy": {
    "startCommand": "echo '=== ENV VARS ===' && printenv | grep -E 'API|KEY|TOKEN' || echo 'No API/KEY/TOKEN vars found' && echo '=== STARTING ===' && cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
